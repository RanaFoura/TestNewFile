I created This file in remote to show that This command ``` git branch -r -v ``` tells me the different between remote and local and added files

And Note that in txt4 i will created it just for check updates that changed by edit fle text and not by adding new file 
